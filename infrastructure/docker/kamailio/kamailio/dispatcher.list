# Dispatcher configuration for Kamailio
# setid(int) destination(sip uri) flags(int,opt) priority(int,opt) attributes(str,opt)
#
# Empty for initial deployment - carriers will be added via API/database
