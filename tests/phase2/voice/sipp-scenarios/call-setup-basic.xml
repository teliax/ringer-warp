<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- Basic Call Setup with RTP -->
<scenario name="Basic Call with Media">

  <!-- Send INVITE -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Test User" <sip:1001@[local_ip]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:1001@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: SIPp Test UAC
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=SIPp Test Call
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 18 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:18 G729/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=ptime:20
      a=maxptime:150
      a=sendrecv
    ]]>
  </send>

  <!-- Receive 100 Trying -->
  <recv response="100" optional="true"/>

  <!-- Receive 180 Ringing -->
  <recv response="180" optional="true"/>

  <!-- Receive 183 Session Progress (Early Media) -->
  <recv response="183" optional="true">
    <action>
      <ereg regexp="c=IN IP[^0-9]*([0-9\.]+)"
            search_in="body"
            assign_to="remote_media_ip"/>
      <ereg regexp="m=audio ([0-9]+)"
            search_in="body"
            assign_to="remote_media_port"/>
    </action>
  </recv>

  <!-- Receive 200 OK -->
  <recv response="200" rtd="true">
    <action>
      <ereg regexp="c=IN IP[^0-9]*([0-9\.]+)"
            search_in="body"
            assign_to="remote_media_ip"/>
      <ereg regexp="m=audio ([0-9]+)"
            search_in="body"
            assign_to="remote_media_port"/>
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Test User" <sip:1001@[local_ip]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:1001@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Play RTP media -->
  <nop>
    <action>
      <exec play_pcap_audio="/home/daldworth/repos/ringer-warp/tests/phase2/test-data/test_audio.pcap"/>
    </action>
  </nop>

  <!-- Call duration pause (10 seconds) -->
  <pause milliseconds="10000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Test User" <sip:1001@[local_ip]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:1001@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Receive 200 OK for BYE -->
  <recv response="200" crlf="true"/>

  <!-- Response time and call length statistics -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 200, 500, 1000, 2000"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000, 20000"/>

</scenario>